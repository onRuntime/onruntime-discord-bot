import { MessageEmbed } from "discord.js";

const createMessageEmbed = (): MessageEmbed => {
    const embed: MessageEmbed = new MessageEmbed();

    return embed;
};

export default createMessageEmbed;