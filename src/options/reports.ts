import { DixtPluginReportsOptions } from "dixt-plugin-reports";

import CHANNELS from "../constants/channels";

const dixtPluginReportsOptions: DixtPluginReportsOptions = {
  channel: CHANNELS.ONRUNTIME.TEAM.LEAD.REPORTS,
};

export default dixtPluginReportsOptions;
